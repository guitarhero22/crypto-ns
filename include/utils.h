#ifndef __UTILS_H__
#define __UTILS_H__

#include<random>
#include<chrono>

double _exp(double mean = 1.0);

// Declare utils for transactions also here, dont make another file
// Keep a Union for transactions and then define util functions to convert that union into strings and vice versa
// Decide how long the transaction ID will be ??????

// Form Topology

#endif